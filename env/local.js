'use strict';
const fs = require('fs');
/**
 * local.js
 *
 * Local config file for running server on a development machine or laptop
 *
 */
let config = {
    port: 3000,
    log_level: 'debug',
};

module.exports = config;